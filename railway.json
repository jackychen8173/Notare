{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "eval \"$(mise env -s bash)\" && chmod +x mvnw && ./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install -Pproduction"
  }
}
